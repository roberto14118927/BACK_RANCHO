version: '3.9'

services:

  postgres:
    restart: always
    image: postgres:14.4-alpine3.16
    ports:
      - 5431:5432
    volumes:
      - db-rancho:/var/lib/postgresql/data
    environment:
      - POSTGRES_NAME=rancho
      - POSTGRES_USER=ranchouser
      - POSTGRES_PASSWORD=admin

  django:
    restart: always
    build: .
    ports:
      - 8000:8000
    volumes:
      - .:/app
    depends_on:
      - postgres

volumes:
  db-rancho:
